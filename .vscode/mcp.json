{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ],
      "description": "瀏覽器自動化工具。當 fetch 被擋（如 LinkedIn）或需要執行 JS 時使用。"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "description": "邏輯導引工具。確保 Copilot 按照「抓取 -> 執行指令 -> 寫入檔案 -> 生成程式碼」的順序思考，不跳步驟。"
    },
    "shell-executor": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-shell"
      ],
      "description": "執行 pnpm cv 指令及資料庫操作。"
    },
    "file-manager": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "./"
      ],
      "description": "精準讀寫 app/(download-pdf) 下的檔案與 jd.md。"
    }
  }
}